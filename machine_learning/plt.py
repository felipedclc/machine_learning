# plt.scatter(x_teste2[:, 0], y_teste2, color="black")  # plot scatters
# plt.plot(x_teste2[:, 0], y_pred, color="red", linewidth=2)  # plot line

# plt.xticks(())
# plt.yticks(())

# plt.show()


# fig, ax = plt.subplots(1, 2, figsize=(12, 5))
# for idx, group in enumerate([("Train", y_treino), ("Test", y_teste)]):
#     data = group[1].value_counts()
#     sns.barplot(ax=ax[idx], x=data.index, y=data.values)
#     ax[idx].set_title(f"{group[0]} Label Count")
#     ax[idx].set_xlabel(f"{group[0]} Labels")
#     ax[idx].set_ylabel("Label Count")

# plt.show()
